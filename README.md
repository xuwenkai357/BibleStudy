
### 💡 如何在下次同时调用这两个技能？

下次您再拿到一个新的查经录音时，不需要再像今天这样一点点试错和分步指导，您只需要发出一段类似下面这样的**“指令组合拳”**，我就会自动触发这两个技能打配合：

**你可以直接复制这段话作为下次的指令模板：**

> “请帮我处理这份查经录音文件： `@[/Users/xdok/Documents/GitHub/查经/以斯帖记/0808_以斯帖记预查.mp3]`
>
> 1. 首先，请调用 `parse_large_audio` 技能，处理这个音频文件以绕过环境格式限制并读取到原文。
> 2. 接着，直接调用 `generate_bible_study_notes` 技能，为我生成那三份标准的查经文档。
> 3. **补充参数**：本次主领是某某，最后做总结的是某某；主要面对的是慕道友和初信群体；查经的主题侧重于‘神在暗中的保守’。
> 
> 请在后台执行转码后，直接帮我把文件保存到对应的书卷目录里。”

一旦收到这个指令，我就会：
1. 立即知道不能直接用 `view_file`，而是乖乖去后台把 mp3 转为 mp4。
2. 并在转码成功后读取原文。
3. 严格按照受众（慕道友）和侧重点，为您输出语言相对通俗、注重福音基础的完美三件套！
